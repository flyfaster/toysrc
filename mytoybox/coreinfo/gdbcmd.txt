%s -batch -ex "set logging file coreinfo.txt" -ex "set logging on" -ex "set pagination off"  -ex "file %s" -ex "core-file %s" -ex "bt 15" -ex "info proc" -ex "printf \"*\n* Libraries \n*\n\"" -ex "info sharedlib" -ex "printf \"*\n* Memory map \n*\n\"" -ex "info target" -ex "printf \"*\n* Registers \n*\n\"" -ex "info registers" -ex "printf \"*\n* Current instructions \n*\n\"" -ex "x/16i \$pc" -ex "printf \"*\n* Threads (full) \n*\n\"" -ex "info threads" -ex "bt" -ex "thread apply all bt full" 	-ex "printf \"*\n* Threads (basic) \n*\n\"" -ex "info threads" 	-ex "thread apply all bt" -ex quit

